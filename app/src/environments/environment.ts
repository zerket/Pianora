export const environment = {
  production: false,
  useMock: false,  // Set to false when testing with real hardware
  wsUrl: '',  // Empty = use window.location.hostname
  apiUrl: ''  // Empty = use window.location.hostname
};
